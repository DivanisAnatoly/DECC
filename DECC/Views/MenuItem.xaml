<UserControl x:Class="DECC.Views.MenuItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:DECC.Views"
             mc:Ignorable="d">
    <materialDesign:Card BorderThickness="2" Margin="10" Background="{DynamicResource PrimaryHueMidBrush}" Height="200" Width="200" Cursor="Hand" MouseDoubleClick="OpenWindow">
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock HorizontalAlignment="Center">
                <materialDesign:PackIcon Kind="{Binding Kind}" Height="100" Width="100" Foreground="{DynamicResource MaterialDesignBodyLight}" />
            </TextBlock>
            <TextBlock FontSize="24" Text="{Binding Text}" Foreground="{DynamicResource MaterialDesignBodyLight}" HorizontalAlignment="Center"></TextBlock>
        </StackPanel>
    </materialDesign:Card>
</UserControl>
